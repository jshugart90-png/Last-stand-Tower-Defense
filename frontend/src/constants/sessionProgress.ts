/** Kills in a single run required to earn one “slaughter” triumph toward the session bounty. */
export const SESSION_SLAUGHTER_WIN_KILLS = 75; // 2.3: was 150 — unreachable for new players (~37 kills on run 1)

/** How many slaughter runs (150+ kills each) open the session bounty chest. */
export const SESSION_BOUNTY_TRIUMPHS_NEEDED = 1; // 2.3: was 2 — first chest should land in session one or two
