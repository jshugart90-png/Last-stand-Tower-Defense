/**
 * Metro resolves `adService.native.ts` / `adService.web.ts` at bundle time.
 * This entry exists for TypeScript and tooling that do not apply platform extensions.
 */
export * from './adService.web';
